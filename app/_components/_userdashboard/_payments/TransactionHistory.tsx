/**
 * TransactionHistory - Main transaction history table component
 * 
 * This is the entry point component. All sub-components are decomposed
 * and live in the same folder for the payments feature.
 * 
 * @see TransactionTable.tsx for the main component implementation
 */
export { default } from "./TransactionTable";